## API Report File for "@memo28/utils"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Equal } from '@memo28/types';
import { Handler } from 'mitt';
import { str } from '@memo28/types';

// @public
export class AnomalousChain {
    protected getErrors(): ErrorsNewResult | null;
    protected recover(): void;
    protected setErrors(err: ErrorsNewResult): void;
    protected skip(errors: ErrorsNewResult | null): this;
}

// @public
export class Chinese extends VerificationFlow<str> {
    constructor(s: str, msg?: string | undefined);
    // (undocumented)
    verification(args?: str): Panic<str>;
}

// @public
export class Emoji extends VerificationFlow<str> {
    constructor(phone?: str, msg?: string | undefined);
    // (undocumented)
    verification(args?: string): Panic<string>;
}

// @public
export class Errors {
    static As(...errors: ErrorsNewResult[]): boolean;
    static Is(value: any): boolean;
    static New(msg: string, opt?: NewOpt): ErrorsNewResult;
}

// @public
export interface ErrorsNewResult {
    info(): ErrorsNewResultInfo;
    trace(): string;
    unWrap(): string;
}

// @public (undocumented)
export interface ErrorsNewResultInfo extends Pick<NewOpt, 'classify'> {
    msg: string;
}

// @public
export class Injection<K = string> {
    constructor(target?: object);
    getKeys(): K[];
    getValue<V = unknown>(key: K): V;
    setTarget(target: object): this;
    setValue<V = unknown>(key: K, value: V): this;
}

// @public
export function isArrayEmpty(val: unknown[]): boolean;

// @public
export function isEmpty(val: any): boolean;

// @public
export function isObjectEmpty(val: object): boolean;

// @public
export class Mail extends VerificationFlow<str> {
    constructor(phone?: str, msg?: string | undefined);
    // (undocumented)
    verification(args?: string): Panic<string>;
}

// @public (undocumented)
export interface NewOpt {
    classify?: number | string;
}

// @public (undocumented)
export function NumberDerived(): void;

// @public
export type Panic<T = unknown> = [ErrorsNewResult | undefined | false | null | true, T];

// @public
export function panicProcessing(opt?: Partial<panicProcessingOpt>): (target: any, key: string, desc: TypedPropertyDescriptor<any>) => void;

// @public (undocumented)
export interface panicProcessingOpt {
    // (undocumented)
    onError: (error: ErrorsNewResult) => void;
    // (undocumented)
    onRecover: (error: ErrorsNewResult) => boolean;
}

// @public
export class Phone extends VerificationFlow<str> {
    constructor(phone?: str, msg?: string | undefined);
    // (undocumented)
    verification(args?: string): Panic<string>;
}

// @public
export function readingWritingSeparationDecor(target: object, key: string): void;

// @public
export type readingWritingSeparationUtilsType<T extends object> = {
    [K in keyof Required<T> as `get${Capitalize<string & K>}`]: () => Required<T>[K];
} & {
    [K in keyof Required<T> as `set${Capitalize<string & K>}`]: (value: Required<T>[K]) => void;
};

// @public
export function SNI(n: number | string | (number | string)[], value: any): boolean;

// @public (undocumented)
export function StringDerived(): void;

// @public
export class ValidationErrorCollection<T extends object> {
    // Warning: (ae-forgotten-export) The symbol "keysForStoreEmit" needs to be exported by the entry point index.d.ts
    // Warning: (ae-forgotten-export) The symbol "storeEmit" needs to be exported by the entry point index.d.ts
    // Warning: (ae-forgotten-export) The symbol "reportType" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    registrationListeningError<K extends keysForStoreEmit | keyof T>(type: K, cb: Handler<Equal<T[K], unknown> extends true ? storeEmit[K] : reportType<T[K]>>): this;
}

// Warning: (ae-forgotten-export) The symbol "Verify" needs to be exported by the entry point index.d.ts
//
// @public
export class VerificationFlow<T = unknown> extends AnomalousChain implements Verify<T> {
    constructor(args: T);
    // (undocumented)
    continuousReporting(type: keysForStoreEmit, value: storeEmit[keysForStoreEmit]): this;
    get(): T;
    set(args: T): this;
    // (undocumented)
    protected skip(errors: ErrorsNewResult | null): this;
    // (undocumented)
    value: T;
    verification(args?: T): Panic<T>;
}

```
