## API Report File for "@memo28/types"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export interface AutomaticAssembly<T = object, R = unknown> {
    // (undocumented)
    config: T | undefined;
    // (undocumented)
    getAssemblyCompleted(): R;
    // (undocumented)
    readConfiguration(res?: T): this;
}

// @public
export type bool = boolean;

// @public
export type Equal<X, Y> = (<T>() => T extends X ? 1 : 2) extends <T>() => T extends Y ? 1 : 2 ? true : false;

// @public
export type Expect<T extends true> = T;

// @public
export type Extends<E, V> = E extends V ? true : false;

// @public
export type fn<P extends any[] = any, R = unknown> = (...args: P) => R;

// @public
export type getKeys<T extends obj> = Equal<T, any[]> extends true ? never : keyof T;

// @public
export type getValues<T extends obj> = Equal<T, any[]> extends true ? never : T[keyof T];

// @public
export type int = number;

// @public
export type IsAny<T> = 0 extends 1 & T ? true : false;

// @public
export type IsFalse<T extends false> = T;

// @public
export type mergeFnWithPromiseFn<T = unknown, P extends any[] = any, isP extends boolean | undefined = undefined> = isP extends undefined ? fn<P, T> | promiseFn<P, T> : isP extends true ? promiseFn<P, T> : fn<P, T>;

// @public
export type obj = object;

// @public
export type objWithValue<T> = {
    [key: string]: T;
};

// @public
export type promiseFn<P extends any[] = any, R = unknown> = (...args: P) => Promise<R>;

// @public
export type str = string;

// @public
export type SuperObject<T extends obj> = T extends any[] ? never : T extends object ? {
    allKeys: getKeys<T>;
    allValues: getValues<T>;
} : never;

```
